<%= simple_form_for [house, monster] do |f| %>
  <%= f.input :name %>
  <!-- NO more string URLs -->
  <%#= f.input :image_url %>
  <%= f.input :photo, as: :file %>
  <%= f.submit 'Condemn', class: 'btn btn-danger mt-2' %>
<% end %>
